<%= simple_form_for(@prework_answer) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :prework_question %>
    <%= f.association :user %>
    <%= f.input :answer %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
