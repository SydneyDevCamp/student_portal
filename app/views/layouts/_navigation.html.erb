<div class="container">
  <div class="row">
    <div class="span12">
      <div class="navbar navbar-inverse">
        <nav class="navbar-inner">
          <div class="container">
            <ul class="nav">
              <li><%= link_to "Sydney Dev Camp - Student Portal", root_path %></li>
            </ul>
            <ul class="nav pull-right">
              
              <% if user_signed_in? %>
                <li class="active">
                  <a href="#"> Menu</a>
                  <ul>
                    <li><%= link_to 'Discussion', forums_root_path %></li>
                    <li><%= link_to 'Prework', prework_path %></li>
                    <li><%= link_to 'Course', course_path %></li>
                  </ul>
                <li>
                <%= link_to 'My account', edit_user_registration_path %>
                </li>
                <li>
                <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
                </li>
                <% if current_user.has_role? :admin %>
                  <li>
                  <%= link_to 'Admin', users_path %>
                  </li>
                <% end %>
              <% else %>
                <li>
                <%= link_to 'Login', new_user_session_path %>
                </li>
              <% end %>
            </ul>
          </div>
        </nav>
      </div>
    </div>
  </div>
</div>
